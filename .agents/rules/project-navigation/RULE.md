# Maintain project navigation

Read `.agents/INDEX.md` to find information and change targets. Update its paths and short descriptions in the same change that adds, moves, removes, or repurposes important files or folders. Maintain `.agents/RULES.md` when rules change. Link to authoritative files instead of duplicating instructions.
